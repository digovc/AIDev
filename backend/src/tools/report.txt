Completes the current job and generates a report.

**Usage:**
*   Use this tool to summarize job results, including errors.
*   Always use this to generate the report when a job completes or an error occurs.
*   Use only once, as the last tool in the conversation.